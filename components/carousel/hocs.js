import { withAxs } from 'axs';
import { ViewPager, Frame, Track, View, AnimatedView } from 'react-view-pager';

export const StyledViewPager = withAxs(ViewPager);
export const StyledFrame = withAxs(Frame);
export const StyledTrack = withAxs(Track);
export const StyledView = withAxs(View);
export const StyledAnimatedView = withAxs(AnimatedView);
