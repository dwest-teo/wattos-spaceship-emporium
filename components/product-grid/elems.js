import { styled } from 'styletron-react';

export const Container = styled('div', {
  display: 'flex',
  flexFlow: 'row wrap',
  padding: '12px',
  justifyContent: 'space-around',
  alignItems: 'center',
});
